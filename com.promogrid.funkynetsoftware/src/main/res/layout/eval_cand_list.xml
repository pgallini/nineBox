<!--
  Copyright 2017 Paul Gallini

  -->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_main"
    android:divider="?android:dividerHorizontal"

    android:orientation="vertical"
    tools:context="nineBoxCandidates.CandidatesListActivity">

    <include
        android:id="@+id/tool_bar"
        layout="@layout/tool_bar"></include>

    <ListView
        android:id="@+id/eval_cand_list"
        style="@style/ScrollList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@+id/tool_bar" />

    <LinearLayout
        style="@style/MainButtonLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/cancel_button"
            style="@style/MainButton"
            android:background="@color/mutedbutton_color"
            android:text="@string/done_button" />

    </LinearLayout>

</LinearLayout>
